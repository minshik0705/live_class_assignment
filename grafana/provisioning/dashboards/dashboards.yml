# 대시보드 파일 프로비저닝 — /var/lib/grafana/dashboards 의 JSON을 자동 로드.
apiVersion: 1

providers:
  - name: event-dashboards
    orgId: 1
    folder: ""
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
